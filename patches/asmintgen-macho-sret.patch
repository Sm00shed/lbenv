diff --git a/Libraries/LibJS/AsmIntGen/src/codegen_x86_64.rs b/Libraries/LibJS/AsmIntGen/src/codegen_x86_64.rs
index 43f72e3324..9e9512dba4 100644
--- a/Libraries/LibJS/AsmIntGen/src/codegen_x86_64.rs
+++ b/Libraries/LibJS/AsmIntGen/src/codegen_x86_64.rs
@@ -860,18 +860,6 @@ fn emit_instruction(
                     w!(out, "    call r11");
                     w!(out, "    mov rax, QWORD PTR {WIN64_RAW_NATIVE_RETURN_SLOT}");
                     w!(out, "    mov rdx, QWORD PTR {WIN64_RAW_NATIVE_VARIANT_SLOT}");
-                } else if matches!(program.object_format, ObjectFormat::MachO) {
-                    // sret: hidden pointer in rdi, VM& in rsi. Reserve a 16-byte
-                    // return slot on the stack; rsp is already 16-byte aligned
-                    // at this point, so sub rsp, 16 preserves the alignment the
-                    // call requires.
-                    w!(out, "    sub rsp, 16");
-                    w!(out, "    mov rdi, rsp");
-                    emit_load_vm(out, "rsi", abi);
-                    w!(out, "    call r11");
-                    w!(out, "    mov rax, QWORD PTR [rsp]");
-                    w!(out, "    mov rdx, QWORD PTR [rsp + 8]");
-                    w!(out, "    add rsp, 16");
                 } else {
                     // Register return: VM& in rdi, result lands in rax (payload)
                     // and rdx (tag).
